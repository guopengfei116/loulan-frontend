const DEVELOP = true;

(function() {

    // 域名
    if(DEVELOP) {
        app.constant("domain", "http://manage.loulan.com");
    } else {
        app.constant("domain", "http://manage.loulan.com");
    }

}());
